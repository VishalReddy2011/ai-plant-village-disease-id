# Frontend

1. cd `frontend`  
2. `npm install`  
3. `npm run dev`

For build  
`npm run build`

# Backend

1. `pip install uv`  
2. cd `backend`  
3. `uv sync`  
4. `uv run uvicorn main:app --reload`
